---
phase: 05-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Travellify/Models/Activity.swift
  - Travellify/Shared/ReminderLeadTime.swift
  - Travellify/Shared/ReminderFireDate.swift
  - TravellifyTests/ReminderSchemaTests.swift
  - TravellifyTests/ReminderFireDateTests.swift
  - Travellify.xcodeproj/project.pbxproj
autonomous: true
requirements: [ACT-07]
must_haves:
  truths:
    - "Activity model exposes isReminderEnabled: Bool (default false) and reminderLeadMinutes: Int? (default nil)"
    - "ReminderLeadTime enum provides exactly four preset raw values: 15, 60, 180, 1440"
    - "ReminderFireDate.fireDate(for:) returns startAt - leadMinutes*60 when reminder enabled, else nil"
    - "SchemaV1 still registers exactly 6 models; TravellifyMigrationPlan.stages is empty"
  artifacts:
    - path: "Travellify/Models/Activity.swift"
      provides: "Activity with reminder fields (additive, defaults)"
      contains: "var isReminderEnabled: Bool = false"
    - path: "Travellify/Shared/ReminderLeadTime.swift"
      provides: "Preset lead-time enum (15/60/180/1440)"
      exports: ["ReminderLeadTime"]
    - path: "Travellify/Shared/ReminderFireDate.swift"
      provides: "Pure fireDate(for:) helper"
      exports: ["ReminderFireDate"]
    - path: "TravellifyTests/ReminderSchemaTests.swift"
      provides: "Grep-gated schema invariants for D52"
    - path: "TravellifyTests/ReminderFireDateTests.swift"
      provides: "Pure-fn coverage for fireDate"
  key_links:
    - from: "Activity.swift"
      to: "SchemaV1"
      via: "extension TravellifySchemaV1 @Model"
      pattern: "extension TravellifySchemaV1"
    - from: "ReminderFireDate.fireDate"
      to: "Activity.startAt + reminderLeadMinutes"
      via: "addingTimeInterval(-TimeInterval(minutes * 60))"
      pattern: "addingTimeInterval\\(-TimeInterval"
---

<objective>
Land additive schema fields on `Activity` per D52, add the two pure-value/pure-function helpers Wave 2 depends on (`ReminderLeadTime`, `ReminderFireDate`), and ship grep-gated schema tests + fireDate unit tests.

Purpose: Wave 2 (NotificationScheduler) requires these two helpers and the new Activity fields. Keep schema additive — stay in SchemaV1, no migration stage.
Output: Activity gains 2 stored properties with defaults; 2 new files in `Travellify/Shared/`; 2 new test files; 12 new pbxproj entries (3 source × 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/05-notifications/05-CONTEXT.md
@.planning/phases/05-notifications/05-RESEARCH.md
@.planning/phases/05-notifications/05-PATTERNS.md

<interfaces>
<!-- Current Activity shape (Travellify/Models/Activity.swift) -->
```swift
extension TravellifySchemaV1 {
    @Model
    final class Activity {
        var id: UUID = UUID()
        var trip: Trip?
        var title: String = ""
        var startAt: Date = Date()
        var location: String?
        var notes: String?
        var createdAt: Date = Date()
        init() {}
    }
}
```

<!-- SchemaV1 registered models (6): Trip, Destination, Document, PackingItem, PackingCategory, Activity -->
<!-- Existing SchemaTests.swift asserts TravellifySchemaV1.models.count == 6 and TravellifyMigrationPlan.stages.isEmpty -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Activity model with reminder fields + write ReminderSchemaTests</name>
  <read_first>
    - Travellify/Models/Activity.swift (the file being modified)
    - Travellify/Models/SchemaV1.swift (confirm 6-model registration unchanged)
    - TravellifyTests/SchemaTests.swift (grep-gate pattern to mirror)
    - .planning/phases/05-notifications/05-CONTEXT.md §"D52 — Schema additions on Activity"
    - .planning/phases/05-notifications/05-PATTERNS.md §"Activity.swift (MODIFIED — additive)" and §"ReminderSchemaTests.swift"
    - .planning/CONVENTIONS.md §"SwiftData — CloudKit-safe rules"
  </read_first>
  <behavior>
    - Test 1: Activity.swift source contains exact string `var isReminderEnabled: Bool = false`
    - Test 2: Activity.swift source contains `var reminderLeadMinutes: Int?`
    - Test 3: Activity.swift source does NOT contain `@Attribute(.unique)` or `@Attribute(.externalStorage)`
    - Test 4: TravellifySchemaV1.models.count == 6
    - Test 5: TravellifyMigrationPlan.stages.isEmpty
    - Test 6: A freshly-constructed Activity() has isReminderEnabled == false and reminderLeadMinutes == nil after save
  </behavior>
  <action>
    Edit `Travellify/Models/Activity.swift` — append two stored properties after `var createdAt: Date = Date()` (line 16) and BEFORE `init() {}` (line 18). Exact insertion:

    ```swift
            // Phase 5 additions (D52) — additive, defaults ensure SwiftData lightweight
            // migration stays inside SchemaV1. CloudKit-safe (no @Attribute, no .unique).
            var isReminderEnabled: Bool = false
            var reminderLeadMinutes: Int? = nil
    ```

    Do NOT alter existing lines. Do NOT add `@Attribute` or relationships. Do NOT bump SchemaV1 model count.

    Create `TravellifyTests/ReminderSchemaTests.swift` (mirror `TravellifyTests/SchemaTests.swift` structure). Use `@MainActor struct ReminderSchemaTests` with Swift Testing `@Test` funcs. Include these exact tests:

    1. `activityHasReminderFieldsWithDefaults()` — reads `"Travellify/Models/Activity.swift"` via `String(contentsOfFile:encoding: .utf8)`, guards with `guard let src = try? ... else { return }` (matches SchemaTests.swift:30–31 precedent), asserts `src.contains("var isReminderEnabled: Bool = false")`, `src.contains("var reminderLeadMinutes: Int?")`, `!src.contains("@Attribute(.unique)")`, `!src.contains("@Attribute(.externalStorage)")`.
    2. `schemaV1StillHasSixModels()` — `#expect(TravellifySchemaV1.models.count == 6)`.
    3. `migrationPlanStillHasNoStages()` — `#expect(TravellifyMigrationPlan.stages.isEmpty)`.
    4. `newActivityDefaultsAreReminderOff()` — in-memory ModelContainer with all 6 models (Trip, Destination, Document, PackingItem, PackingCategory, Activity), insert `Activity()`, save, assert `a.isReminderEnabled == false` and `a.reminderLeadMinutes == nil`.

    Register `ReminderSchemaTests.swift` in `Travellify.xcodeproj/project.pbxproj` with 4 entries (PBXBuildFile, PBXFileReference, PBXGroup child in TravellifyTests group, PBXSourcesBuildPhase entry on TravellifyTests target). Use `TravellifyTests/DayLabelTests.swift` pbxproj entries as the exact template (lines 51, 92, 264, 460 per PATTERNS.md). Generate fresh UUIDs (16-hex-char AD-prefixed, uniqueness-checked against existing UUIDs).
  </action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project Travellify.xcodeproj -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:TravellifyTests/ReminderSchemaTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var isReminderEnabled: Bool = false" Travellify/Models/Activity.swift` returns 1
    - `grep -c "var reminderLeadMinutes: Int?" Travellify/Models/Activity.swift` returns 1
    - `grep -c "@Attribute" Travellify/Models/Activity.swift` returns 0
    - `grep -c "ReminderSchemaTests.swift" Travellify.xcodeproj/project.pbxproj` returns 4 (4 entries)
    - xcodebuild exits 0; ReminderSchemaTests all four tests pass
    - Existing `SchemaTests` still passes (model count, CloudKit-safety grep gates unchanged)
  </acceptance_criteria>
  <done>Activity has additive D52 fields with defaults; ReminderSchemaTests file registered in pbxproj; all four schema tests + pre-existing SchemaTests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ReminderLeadTime + ReminderFireDate helpers + ReminderFireDateTests</name>
  <read_first>
    - Travellify/Shared/ActivityDateLabels.swift (slot-mate pattern — imports, @MainActor inference, static members)
    - TravellifyTests/DayLabelTests.swift (pure-fn test boilerplate)
    - .planning/phases/05-notifications/05-RESEARCH.md §"ReminderLeadTime preset enum" (lines 572–590) and §"fireDate helper" (lines 559–567)
    - .planning/phases/05-notifications/05-PATTERNS.md §"ReminderLeadTime.swift" and §"ReminderFireDateTests.swift"
    - .planning/CONVENTIONS.md §"project.pbxproj — hand-written, 4 entries per file"
  </read_first>
  <behavior>
    - Test 1: ReminderLeadTime.allCases.map(\.rawValue) == [15, 60, 180, 1440]
    - Test 2: ReminderLeadTime.default == .oneHour (raw 60) per D51
    - Test 3: fireDate(for:) returns startAt minus (leadMinutes * 60) seconds when isReminderEnabled && reminderLeadMinutes != nil
    - Test 4: fireDate returns nil when isReminderEnabled == false
    - Test 5: fireDate returns nil when reminderLeadMinutes == nil
    - Test 6: DST boundary — scheduling lead-time across a spring-forward or fall-back boundary still produces startAt.addingTimeInterval(-TimeInterval(m*60)) (absolute math, not wall-clock — trigger handles DST via calendar components)
  </behavior>
  <action>
    Create `Travellify/Shared/ReminderLeadTime.swift` verbatim from RESEARCH.md §"ReminderLeadTime preset enum":

    ```swift
    import Foundation

    enum ReminderLeadTime: Int, CaseIterable, Identifiable {
        case fifteenMinutes = 15
        case oneHour = 60
        case threeHours = 180
        case oneDay = 1440

        var id: Int { rawValue }
        static let `default`: ReminderLeadTime = .oneHour   // D51

        var label: String {
            switch self {
            case .fifteenMinutes: "15 min before"
            case .oneHour: "1 hour before"
            case .threeHours: "3 hours before"
            case .oneDay: "1 day before"
            }
        }
    }
    ```

    Create `Travellify/Shared/ReminderFireDate.swift`:

    ```swift
    import Foundation

    /// Pure helper. Absolute-time arithmetic: `startAt - leadMinutes*60`.
    /// DST correctness is handled downstream by UNCalendarNotificationTrigger
    /// (per RESEARCH §4). Returns nil when the user hasn't opted in.
    enum ReminderFireDate {
        static func fireDate(for activity: Activity) -> Date? {
            guard activity.isReminderEnabled,
                  let minutes = activity.reminderLeadMinutes else { return nil }
            return activity.startAt.addingTimeInterval(-TimeInterval(minutes * 60))
        }
    }
    ```

    Create `TravellifyTests/ReminderFireDateTests.swift` — Swift Testing `@MainActor struct ReminderFireDateTests` with four @Test funcs covering behaviors 1–6 above. Use fixed `Date` inputs (e.g., `Date(timeIntervalSince1970: 1_700_000_000)`). Construct `Activity()` instances with fields mutated directly — no ModelContainer needed for pure-fn tests.

    Also cover ReminderLeadTime invariants in this same file (or a nested `@Suite` if preferred): raw values `[15, 60, 180, 1440]`, `.default == .oneHour`.

    Register all three new files in `project.pbxproj` (3 files × 4 entries = 12 lines):
    - `ReminderLeadTime.swift` → `Shared/` group, template `ActivityDateLabels.swift` (lines 44, 85, 275, 484 per PATTERNS.md)
    - `ReminderFireDate.swift` → `Shared/` group, same template
    - `ReminderFireDateTests.swift` → `TravellifyTests/` group, template `DayLabelTests.swift` (lines 51, 92, 264, 460)
    Generate fresh unique AD-prefixed UUIDs for each.
  </action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project Travellify.xcodeproj -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:TravellifyTests/ReminderFireDateTests</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Travellify/Shared/ReminderLeadTime.swift` exit 0
    - `test -f Travellify/Shared/ReminderFireDate.swift` exit 0
    - `test -f TravellifyTests/ReminderFireDateTests.swift` exit 0
    - `grep -c "case fifteenMinutes = 15" Travellify/Shared/ReminderLeadTime.swift` returns 1
    - `grep -c "case oneDay = 1440" Travellify/Shared/ReminderLeadTime.swift` returns 1
    - `grep -c "addingTimeInterval(-TimeInterval(minutes \* 60))" Travellify/Shared/ReminderFireDate.swift` returns 1
    - `grep -c "ReminderLeadTime.swift" Travellify.xcodeproj/project.pbxproj` returns 4
    - `grep -c "ReminderFireDate.swift" Travellify.xcodeproj/project.pbxproj` returns 4
    - `grep -c "ReminderFireDateTests.swift" Travellify.xcodeproj/project.pbxproj` returns 4
    - xcodebuild exits 0; ReminderFireDateTests all pass
  </acceptance_criteria>
  <done>ReminderLeadTime + ReminderFireDate compile in the app target; fireDate tests green; pbxproj has 12 new entries (3 files × 4 each).</done>
</task>

</tasks>

<verification>
Full suite green after Wave 1:
```
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project Travellify.xcodeproj -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e'
```

Specifically:
- ReminderSchemaTests (4 tests) green
- ReminderFireDateTests (≥4 tests) green
- Pre-existing SchemaTests, ActivityTests green (no regression from D52 additions)
</verification>

<success_criteria>
- Activity exposes `isReminderEnabled: Bool = false` and `reminderLeadMinutes: Int? = nil` with no @Attribute annotations
- ReminderLeadTime raw values are exactly `{15, 60, 180, 1440}`; default is `.oneHour`
- ReminderFireDate.fireDate returns `startAt - leadMinutes*60` when enabled+leadMinutes set, else nil
- 3 new source files (Activity modified; 2 new in Shared/) + 2 new test files registered with correct 4-entry pbxproj pattern
- SchemaV1.models.count still == 6; TravellifyMigrationPlan.stages still empty
</success_criteria>

<output>
After completion, create `.planning/phases/05-notifications/05-01-SUMMARY.md` documenting:
- Exact D52 fields added
- pbxproj UUIDs used (for downstream reference)
- Any SourceKit stale-diagnostic warnings observed (ignore per CONVENTIONS; trust xcodebuild)
</output>
