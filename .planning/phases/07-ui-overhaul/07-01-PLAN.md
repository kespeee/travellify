---
phase: 07-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Travellify/DesignSystem/Tokens/DSColor.swift
  - Travellify/DesignSystem/Tokens/DSTypography.swift
  - Travellify/DesignSystem/Tokens/DSSpacing.swift
  - Travellify/DesignSystem/Tokens/DSRadius.swift
  - Travellify/DesignSystem/Tokens/DSShadow.swift
  - Travellify/DesignSystem/Modifiers/LiquidGlassModifier.swift
  - Travellify/DesignSystem/Components/LiquidGlassButton.swift
  - Travellify/App/TabBarRoot.swift
  - Travellify/Features/Settings/SettingsPlaceholderView.swift
  - Travellify/TravellifyApp.swift
  - Travellify/App/AppState.swift
  - Travellify/App/AppDelegate.swift
  - Travellify/Assets.xcassets/EmptyStates.xcassets/Contents.json
  - TravellifyTests/DesignSystemTokensTests.swift
  - .planning/phases/07-ui-overhaul/ASSET-PIPELINE.md
  - Travellify.xcodeproj/project.pbxproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "DSColor, DSTypography, DSSpacing, DSRadius, DSShadow namespaces exist under Travellify/DesignSystem/Tokens/ and expose the Figma-derived token set (D7 decisions)"
    - "View.liquidGlass(tint:) modifier exists and uses native .glassEffect() on iOS 26+, .ultraThinMaterial + overlays on iOS 17–25 (D7-05)"
    - "LiquidGlassButton(title:action:tint:) renders the Figma 'Button - Liquid Glass - Text' spec: 40pt height, pill shape, 17pt SF Pro Medium label, #0091FF default tint (D7-05)"
    - "TabBarRoot wraps a 2-tab TabView: Trips tab hosts the existing ContentView body; Settings tab hosts SettingsPlaceholderView (D7-04)"
    - "SettingsPlaceholderView shows a centered gear icon + 'Settings' title + 'Coming in a future update' subtitle (D7-04)"
    - "TravellifyApp's root view is TabBarRoot (not ContentView)"
    - "Existing AppState.PendingDeepLink.activity(UUID) and .trip(UUID) continue to resolve: handler first switches selectedTab to Trips, then pushes onto Trips-tab NavigationStack (D7-04)"
    - "Assets.xcassets contains an empty EmptyStates asset catalog folder prepared for screen-scoped illustrations (D7-06)"
    - "ASSET-PIPELINE.md documents the Figma-MCP asset-ingestion workflow and naming conventions (D7-06)"
    - "Xcode build on iPhone 16e simulator succeeds; DesignSystemTokensTests passes; no Phase 1–6 regressions (requirement preservation gate)"
  artifacts:
    - path: "Travellify/DesignSystem/Tokens/DSColor.swift"
      provides: "Color tokens (accent, label, background, separator)"
      exports: ["DSColor", "DSColor.Accent", "DSColor.Label", "DSColor.Background", "DSColor.Separator"]
    - path: "Travellify/DesignSystem/Tokens/DSTypography.swift"
      provides: "Typography tokens (largeTitle, title2, headline, subheadline, bodyControl)"
      exports: ["DSTypography"]
    - path: "Travellify/DesignSystem/Tokens/DSSpacing.swift"
      provides: "Spacing scale s4/s8/s12/s16/s20/s24/s32"
      exports: ["DSSpacing"]
    - path: "Travellify/DesignSystem/Tokens/DSRadius.swift"
      provides: "Corner-radius tokens (pill=1000, card=12, sheet=16)"
      exports: ["DSRadius"]
    - path: "Travellify/DesignSystem/Tokens/DSShadow.swift"
      provides: "Shadow spec tokens (glass: 0/8/40/0.12)"
      exports: ["DSShadow"]
    - path: "Travellify/DesignSystem/Modifiers/LiquidGlassModifier.swift"
      provides: "View.liquidGlass(tint:) modifier with iOS 26/17–25 branch"
      contains: "@available(iOS 26.0, *)"
    - path: "Travellify/DesignSystem/Components/LiquidGlassButton.swift"
      provides: "LiquidGlassButton(title:action:tint:) component"
      contains: "DSColor.Accent.primary"
    - path: "Travellify/App/TabBarRoot.swift"
      provides: "Two-tab TabView hosting Trips + Settings"
      contains: "TabView(selection:"
    - path: "Travellify/Features/Settings/SettingsPlaceholderView.swift"
      provides: "Settings-tab stub view"
      contains: "Coming in a future update"
    - path: "Travellify/TravellifyApp.swift"
      provides: "Root view swapped to TabBarRoot"
      contains: "TabBarRoot()"
    - path: "TravellifyTests/DesignSystemTokensTests.swift"
      provides: "Smoke tests for token non-nil + accent RGB match"
      contains: "DSColor.Accent.primary"
  key_links:
    - from: "TravellifyApp"
      to: "TabBarRoot"
      via: "WindowGroup body"
      pattern: "TabBarRoot\\(\\)"
    - from: "TabBarRoot Trips tab"
      to: "ContentView (existing)"
      via: "Tab body"
      pattern: "ContentView\\(\\)"
    - from: "AppDelegate deep-link handler"
      to: "TabBarRoot.selectedTab = .trips"
      via: "AppState.pendingDeepLink resolution"
      pattern: "selectedTab"
    - from: "LiquidGlassButton fill"
      to: "DSColor.Accent.primary"
      via: "fill color property"
      pattern: "DSColor\\.Accent"
---

<objective>
Phase 7 Wave 1 — **Design Foundation**. Ship the shared primitives and tab-bar scaffolding that every later sub-phase (07-02 Trips, 07-03 Documents, …) will consume. **No screens are migrated in this wave.** Only plumbing: design tokens, liquid-glass view modifier, liquid-glass button component, 2-tab root refactor (Trips + Settings-stub), asset-pipeline conventions, and a minimal smoke-test suite.

Purpose: Execute CONTEXT decisions D7-01 through D7-07 at the foundation layer — tokens (D7-05 typography/color pulled from Figma node 93:132), tab bar (D7-04), liquid-glass rendering (D7-05), asset pipeline (D7-06). D7-07 (conditional "+" toolbar) is **not** implemented here — it lands with 07-02 Trips since it requires touching TripListView.
Output: ~9 new source files, 2 modified source files (TravellifyApp, AppDelegate/AppState for deep-link re-routing through TabBarRoot), 1 new test file, 1 asset-catalog folder, 1 doc file, and associated pbxproj entries. Xcode build + DesignSystemTokensTests green on iPhone 16e simulator. Zero functional regressions in Phase 1–6 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-overhaul/07-CONTEXT.md

<interfaces>
<!-- Token values extracted from Figma node 93:132 (TripListView empty state) design context. -->
<!-- Font family: "SF Pro" with variation 'wdth' 100 and (where noted) 'ss16' feature setting. -->

Design tokens to materialize (all values from Figma node 93:132):

  DSColor.Accent.primary      = Color(red: 0x00/255, green: 0x91/255, blue: 0xFF/255)  // #0091FF
  DSColor.Label.primary       = Color.white                                            // Labels/Primary
  DSColor.Label.secondary     = Color(red: 235/255, green: 235/255, blue: 245/255, opacity: 0.7)
  DSColor.Label.vibrantPrimary= Color(red: 0xF5/255, green: 0xF5/255, blue: 0xF5/255)  // #F5F5F5 (vibrant)
  DSColor.Background.primary  = Color(red: 0x1C/255, green: 0x1C/255, blue: 0x1E/255)  // #1C1C1E (grouped secondary)
  DSColor.Background.tertiary = Color(red: 0x2C/255, green: 0x2C/255, blue: 0x2E/255)  // #2C2C2E (grouped tertiary)
  DSColor.Background.vibrantTertiaryFill = Color(red: 0x12/255, green: 0x12/255, blue: 0x12/255) // #121212
  DSColor.Separator.nonOpaque = Color.white.opacity(0.17)

  DSTypography.largeTitle = DSFont(name: "SF Pro", size: 34, weight: .bold,     lineHeight: 41, tracking: 0.40)
  DSTypography.title2     = DSFont(name: "SF Pro", size: 22, weight: .bold,     lineHeight: 26, tracking: -0.26)
  DSTypography.headline   = DSFont(name: "SF Pro", size: 17, weight: .semibold, lineHeight: 22, tracking: -0.43)
  DSTypography.subheadline= DSFont(name: "SF Pro", size: 15, weight: .regular,  lineHeight: 20, tracking: -0.23)
  DSTypography.bodyControl= DSFont(name: "SF Pro", size: 17, weight: .medium,   lineHeight: nil, tracking: 0)

  DSSpacing.s4 = 4; s8 = 8; s12 = 12; s16 = 16; s20 = 20; s24 = 24; s32 = 32

  DSRadius.pill  = 1000
  DSRadius.card  = 12
  DSRadius.sheet = 16

  DSShadow.glass = DSShadowSpec(xOffset: 0, yOffset: 8, blur: 40, color: Color.black.opacity(0.12))

Liquid-glass button geometry (Figma node 93:132 "Button - Liquid Glass - Text" I96:1726):
  height: 40
  padding: vertical 6, horizontal 20
  cornerRadius: 1000 (pill)
  label font: DSTypography.bodyControl
  label color: DSColor.Label.vibrantPrimary
  tint fill: DSColor.Accent.primary (blue #0091FF, 100% opacity base)
  shadow: DSShadow.glass

Current root wiring (to be refactored):
  TravellifyApp.swift: WindowGroup { ContentView() }
  ContentView.swift: ~36-line file — NavigationStack-backed root that reads AppState.pendingDeepLink and switches on AppDestination enum (see 06-03 D81 integration).
  AppState.swift: holds `pendingDeepLink: PendingDeepLink?` + `navigationPath: NavigationPath` (or equivalent).
  AppDelegate.swift: parses UNNotificationResponse userInfo, sets AppState.pendingDeepLink.

New root wiring:
  TravellifyApp.swift: WindowGroup { TabBarRoot() }
  TabBarRoot.swift: @State selectedTab: Tab = .trips; TabView(selection: $selectedTab) { ContentView().tag(Tab.trips); SettingsPlaceholderView().tag(Tab.settings) }
  Deep-link handler: inside TabBarRoot's .onReceive/.onChange of AppState.pendingDeepLink, first assign `selectedTab = .trips`, then let ContentView's existing logic push the destination.

iOS-version branch (D7-05):
  #if canImport(SwiftUI) with @available(iOS 26.0, *) check.
  On iOS 26+: call View.glassEffect(.regular, in: shape).tint(tint) or equivalent Apple API (verify exact call at implementation time against Xcode 26 SDK — the actual API may be `.glassEffect(_:in:)` or a capsule-backed style).
  On iOS 17–25 fallback: apply .background(material, in: shape) where material = .ultraThinMaterial, then overlay a gradient fill (rgba(255,255,255,0.06) → rgba(0,0,0,0.6) per Figma style spec) with mix-blend-like compositing via .blendMode(.plusLighter) where needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold DesignSystem tokens</name>
  <read_first>
    - .planning/phases/07-ui-overhaul/07-CONTEXT.md §"Implementation Decisions" (D7-05 glass, D7-06 assets)
    - Travellify/TravellifyApp.swift (root entry — do not modify yet; confirm current structure)
  </read_first>
  <action>
    Create `Travellify/DesignSystem/Tokens/` with 5 token files:

    **DSColor.swift** — Enum namespaces `Accent`, `Label`, `Background`, `Separator` exposing static `Color` properties per the token table in `<interfaces>`. Use `Color(red:green:blue:opacity:)` with Double conversions from 0…255 / 0…1. Example:

    ```swift
    enum DSColor {
      enum Accent { static let primary = Color(red: 0x00/255.0, green: 0x91/255.0, blue: 0xFF/255.0) }
      enum Label { /* primary, secondary, vibrantPrimary */ }
      enum Background { /* primary, tertiary, vibrantTertiaryFill */ }
      enum Separator { static let nonOpaque = Color.white.opacity(0.17) }
    }
    ```

    **DSTypography.swift** — Define a `DSFont` struct `{ name: String, size: CGFloat, weight: Font.Weight, lineHeight: CGFloat?, tracking: CGFloat }` and a `DSTypography` enum exposing static `DSFont` instances for `largeTitle`, `title2`, `headline`, `subheadline`, `bodyControl` per token table. Provide a `.font() -> Font` helper and a `View` modifier `.dsTypography(_:)` that applies `.font()` + `.kerning(tracking)` + (optionally via `.lineSpacing(max(0, lineHeight - defaultLineHeight))`) when `lineHeight` is non-nil. Use `Font.system(size:weight:)` for SF Pro mapping (SF Pro is the system font).

    **DSSpacing.swift** — `enum DSSpacing { static let s4: CGFloat = 4; static let s8: CGFloat = 8; ... s32 = 32 }`

    **DSRadius.swift** — `enum DSRadius { static let pill: CGFloat = 1000; static let card: CGFloat = 12; static let sheet: CGFloat = 16 }`

    **DSShadow.swift** — struct `DSShadowSpec { let xOffset: CGFloat; let yOffset: CGFloat; let blur: CGFloat; let color: Color }`. `enum DSShadow { static let glass = DSShadowSpec(xOffset: 0, yOffset: 8, blur: 40, color: Color.black.opacity(0.12)) }`. Add `View.dsShadow(_:)` helper that calls `.shadow(color:radius:x:y:)`.

    Add all 5 files to `Travellify.xcodeproj/project.pbxproj`:
    - One PBXFileReference per file
    - One PBXBuildFile per file (target: Travellify)
    - Corresponding children entries under the Travellify group (create `DesignSystem/Tokens/` PBXGroup nesting if one doesn't exist)
    - Build-file refs into the PBXSourcesBuildPhase of the Travellify target
  </action>
  <verify>
    Build Travellify on iPhone 16e simulator with `xcodebuild -scheme Travellify -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 16e' build`. Expect zero errors / warnings from the new files. Confirm `DSColor.Accent.primary`, `DSTypography.largeTitle`, `DSSpacing.s16`, `DSRadius.pill`, `DSShadow.glass` all resolve from a SwiftUI preview in any token file.
  </verify>
  <commit>
    feat(07-01): scaffold DesignSystem tokens (color, typography, spacing, radius, shadow)
  </commit>
</task>

<task type="auto">
  <name>Task 2: LiquidGlass modifier + LiquidGlassButton component</name>
  <read_first>
    - Travellify/DesignSystem/Tokens/DSColor.swift (just created)
    - Travellify/DesignSystem/Tokens/DSTypography.swift
    - Travellify/DesignSystem/Tokens/DSRadius.swift
    - Travellify/DesignSystem/Tokens/DSShadow.swift
    - .planning/phases/07-ui-overhaul/07-CONTEXT.md §D7-05
  </read_first>
  <action>
    **Create `Travellify/DesignSystem/Modifiers/LiquidGlassModifier.swift`:**

    ```swift
    import SwiftUI

    struct LiquidGlassModifier<S: Shape>: ViewModifier {
      let shape: S
      let tint: Color?
      func body(content: Content) -> some View {
        if #available(iOS 26.0, *) {
          // Native Apple liquid glass — call the current Xcode 26 SDK API.
          // If the exact API name differs in the SDK, adjust to e.g. `.glassEffect(_:in:)`.
          return AnyView(content.glassEffect(.regular, in: shape).overlay(tintOverlay))
        } else {
          return AnyView(
            content
              .background(.ultraThinMaterial, in: shape)
              .overlay(tintOverlay)
              .overlay(
                shape
                  .fill(LinearGradient(
                    colors: [Color.white.opacity(0.06), Color.black.opacity(0.6)],
                    startPoint: .leading, endPoint: .trailing))
                  .blendMode(.plusLighter)
              )
          )
        }
      }
      @ViewBuilder private var tintOverlay: some View {
        if let tint { shape.fill(tint) }
      }
    }

    extension View {
      func liquidGlass<S: Shape>(in shape: S, tint: Color? = nil) -> some View {
        modifier(LiquidGlassModifier(shape: shape, tint: tint))
      }
    }
    ```

    **IMPORTANT (implementation deviation allowed):** If the Xcode 26 SDK's native API is not `.glassEffect(.regular, in:)` exactly, call the closest available SDK surface and document the deviation in SUMMARY.md. The iOS 26 branch is best-effort; the iOS 17–25 fallback is the canonical visual reference for this wave.

    **Create `Travellify/DesignSystem/Components/LiquidGlassButton.swift`:**

    ```swift
    import SwiftUI

    struct LiquidGlassButton: View {
      let title: String
      let tint: Color
      let action: () -> Void

      init(_ title: String, tint: Color = DSColor.Accent.primary, action: @escaping () -> Void) {
        self.title = title; self.tint = tint; self.action = action
      }

      var body: some View {
        Button(action: action) {
          Text(title)
            .font(.system(size: 17, weight: .medium))
            .foregroundStyle(DSColor.Label.vibrantPrimary)
            .padding(.vertical, DSSpacing.s4 + 2)  // 6
            .padding(.horizontal, DSSpacing.s20)
            .frame(height: 40)
            .frame(minWidth: 0)
        }
        .buttonStyle(.plain)
        .background(tint, in: Capsule())
        .liquidGlass(in: Capsule(), tint: nil)
        .dsShadow(DSShadow.glass)
      }
    }

    #Preview { LiquidGlassButton("Create a trip") {} .padding().background(DSColor.Background.primary) }
    ```

    Add both files to pbxproj (PBXFileReference + PBXBuildFile + group entry + sources-build-phase entry), grouped under `Travellify/DesignSystem/Modifiers/` and `Travellify/DesignSystem/Components/`.
  </action>
  <verify>
    Build succeeds. The `#Preview` for `LiquidGlassButton` renders in Xcode's canvas with blue pill, white 17pt Medium "Create a trip" label, correct 40pt height. Run tests: no regressions.
  </verify>
  <commit>
    feat(07-01): add .liquidGlass() modifier and LiquidGlassButton component
  </commit>
</task>

<task type="auto">
  <name>Task 3: TabBarRoot + Settings placeholder + routing refactor</name>
  <read_first>
    - Travellify/TravellifyApp.swift (root entry — current body)
    - Travellify/ContentView.swift (full file — becomes Trips-tab body; inspect NavigationStack + AppDestination switch)
    - Travellify/App/AppState.swift (full file — confirm PendingDeepLink shape + navigationPath ownership)
    - Travellify/App/AppDelegate.swift (full file — confirm deep-link assign site)
    - Travellify/DesignSystem/Tokens/DSColor.swift (for Settings stub background)
  </read_first>
  <action>
    **Create `Travellify/Features/Settings/SettingsPlaceholderView.swift`:**

    ```swift
    import SwiftUI

    struct SettingsPlaceholderView: View {
      var body: some View {
        VStack(spacing: DSSpacing.s16) {
          Image(systemName: "gearshape.fill")
            .font(.system(size: 56, weight: .regular))
            .foregroundStyle(DSColor.Label.secondary)
          VStack(spacing: DSSpacing.s8) {
            Text("Settings")
              .font(.system(size: 22, weight: .bold))
              .foregroundStyle(DSColor.Label.primary)
            Text("Coming in a future update")
              .font(.system(size: 15, weight: .regular))
              .foregroundStyle(DSColor.Label.secondary)
              .multilineTextAlignment(.center)
          }
        }
        .padding(DSSpacing.s24)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(DSColor.Background.primary, ignoresSafeAreaEdges: .all)
      }
    }
    ```

    **Create `Travellify/App/TabBarRoot.swift`:**

    ```swift
    import SwiftUI

    struct TabBarRoot: View {
      enum Tab: Hashable { case trips, settings }
      @EnvironmentObject private var appState: AppState          // or @Environment(AppState.self) depending on existing style
      @State private var selectedTab: Tab = .trips

      var body: some View {
        TabView(selection: $selectedTab) {
          ContentView()
            .tabItem { Label("Trips", systemImage: "airplane") }
            .tag(Tab.trips)
          SettingsPlaceholderView()
            .tabItem { Label("Settings", systemImage: "gearshape") }
            .tag(Tab.settings)
        }
        .tint(DSColor.Accent.primary)
        .onChange(of: appState.pendingDeepLink) { _, newValue in
          if newValue != nil { selectedTab = .trips }
        }
      }
    }
    ```

    Match the existing `AppState` observability style exactly — if the project uses `@Observable` (iOS 17 macro) rather than `@EnvironmentObject`, use `@Environment(AppState.self) private var appState` and switch the trigger to `.onChange(of: appState.pendingDeepLink)` accordingly (verify from AppState.swift).

    **Edit `Travellify/TravellifyApp.swift`:**
    Replace `ContentView()` inside `WindowGroup { ... }` with `TabBarRoot()`. Preserve every existing modifier (`.modelContainer(...)`, `.environmentObject(appState)` or equivalent, `UIApplicationDelegateAdaptor`).

    **Verify `ContentView.swift` is untouched except for:** If `ContentView` currently applied a `.ignoresSafeArea(.all, edges: .bottom)` or similar that conflicts with the tab bar, remove only that conflict. Otherwise do NOT modify `ContentView` — it now runs as a tab child and its NavigationStack + AppDestination switch must continue functioning identically.

    **Deep-link preservation:** `AppDelegate` and `AppState` logic is unchanged — when a UNNotificationResponse fires, it still sets `appState.pendingDeepLink`. `TabBarRoot.onChange(of: appState.pendingDeepLink)` flips `selectedTab = .trips` before `ContentView`'s existing `.onChange(of: appState.pendingDeepLink)` (unchanged) pushes the destination. If needed, adjust evaluation order so tab switch wins the race — the simplest fix is to have `TabBarRoot` consume the trigger in `.task(id:)` with a tiny `await Task.yield()` before clearing if the existing pattern relies on that.

    Add 3 new files to pbxproj (TabBarRoot.swift, SettingsPlaceholderView.swift, and the new `Features/Settings/` PBXGroup). TravellifyApp.swift is already tracked; only its contents changed.

    **Also:** Create the asset-catalog folder structure. Add `Travellify/Assets.xcassets/EmptyStates.xcassets/` with a minimal `Contents.json` (empty catalog placeholder per Apple asset-catalog schema). Register as a folder reference in pbxproj.
  </action>
  <verify>
    Build succeeds on iPhone 16e simulator. Launch in simulator:
    1. App root renders with a bottom tab bar: Trips (airplane) + Settings (gear); Trips is selected by default.
    2. Switching to Settings shows the centered gear / "Settings" / "Coming in a future update" stub.
    3. Switching back to Trips shows the previously visible `ContentView` content unchanged (trip list, navigation stack intact).
    4. Simulated deep-link test: invoke `appState.pendingDeepLink = .trip(UUID())` in a preview/test, confirm `selectedTab` flips to `.trips` and the Trips tab's NavigationStack pushes the trip detail (existing Phase 6 D81 flow).
    5. All Phase 1–6 tests pass: `xcodebuild test -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e'`.
  </verify>
  <commit>
    feat(07-01): introduce TabBarRoot + Settings placeholder and reroute app root
  </commit>
</task>

<task type="auto">
  <name>Task 4: Asset-pipeline convention doc + smoke tests</name>
  <read_first>
    - Travellify/DesignSystem/Tokens/*.swift (all token files)
    - .planning/phases/07-ui-overhaul/07-CONTEXT.md §"Canonical References"
  </read_first>
  <action>
    **Create `.planning/phases/07-ui-overhaul/ASSET-PIPELINE.md`** with this content:

    ```
    # Phase 7 Asset Pipeline

    Every Figma delivery that introduces a raster illustration follows this pipeline.

    ## Location
    `Travellify/Assets.xcassets/EmptyStates.xcassets/<screen>/`

    ## Naming
    - Image Set: `empty-state-<screen>` (e.g. `empty-state-trips`, `empty-state-documents`)
    - Files: `empty-state-<screen>.png` / `@2x.png` / `@3x.png`

    ## Ingestion (from Figma MCP)
    1. Identify the illustration node in the Figma file (e.g. node `96:870` for TripListView empty state).
    2. Call `get_design_context(fileKey="jLfc8jUHe465DkmibqqSjo", nodeId="<nodeId>")` or `get_screenshot(...)` to receive the rendered illustration.
    3. If the response returns a bundled PNG URL under `localhost`, download it at 1x, 2x, and 3x resolutions.
    4. Save into `Travellify/Assets.xcassets/EmptyStates.xcassets/<screen>/` with the naming above. Generate a `Contents.json` that maps each `idiom: "universal"` + scale variant.
    5. Reference in code as `Image("empty-state-<screen>")`.

    ## Naming sourcemap
    Keep an `.planning/phases/07-ui-overhaul/ASSET-SOURCEMAP.md` entry each time an asset is added:
    | Asset name | Figma node | Delivery date | Consumed by (PLAN) |
    |------------|-----------|---------------|---------------------|

    ## When NOT to use raster assets
    - Icons that exist in SF Symbols → use `Image(systemName:)`.
    - Tokens/shapes/text → use SwiftUI primitives.
    - Asset bitmaps are reserved for illustrations that can't be composed (e.g. the 3-tilted-card TripListView illustration at node 96:870).
    ```

    **Create `TravellifyTests/DesignSystemTokensTests.swift`** using Swift Testing:

    ```swift
    import Testing
    import SwiftUI
    @testable import Travellify

    @Suite("Design System — Tokens")
    struct DesignSystemTokensTests {
      @Test("DSColor.Accent.primary is the Figma #0091FF blue")
      func accentPrimaryMatchesFigma() throws {
        let c = DSColor.Accent.primary.resolve(in: EnvironmentValues()).linear
        // Tolerance on linear-space roundtrip
        #expect(abs(c.red   - 0x00/255.0) < 0.01)
        #expect(abs(c.green - 0x91/255.0) < 0.3)   // linear ≠ sRGB for non-zero channels
        #expect(abs(c.blue  - 0xFF/255.0) < 0.02)
      }

      @Test("Typography scale exposes all five roles")
      func typographyScaleExists() {
        #expect(DSTypography.largeTitle.size == 34)
        #expect(DSTypography.title2.size == 22)
        #expect(DSTypography.headline.size == 17)
        #expect(DSTypography.subheadline.size == 15)
        #expect(DSTypography.bodyControl.size == 17)
      }

      @Test("Spacing scale monotonic")
      func spacingScaleIsMonotonic() {
        let scale: [CGFloat] = [DSSpacing.s4, DSSpacing.s8, DSSpacing.s12, DSSpacing.s16, DSSpacing.s20, DSSpacing.s24, DSSpacing.s32]
        #expect(scale == scale.sorted())
      }

      @Test("Radius pill is ≥ 999 for full rounding")
      func pillRadiusRoundsFully() {
        #expect(DSRadius.pill >= 999)
      }

      @Test("Glass shadow matches Figma rgba(0,0,0,0.12) y=8 blur=40")
      func glassShadowValues() {
        #expect(DSShadow.glass.yOffset == 8)
        #expect(DSShadow.glass.blur == 40)
      }
    }
    ```

    **NOTE — `resolve(in:)` API:** If `Color.resolve(in:)` is not available in the target SDK, fall back to instantiating a `UIColor(Color(...))` round-trip and reading `getRed(...)`. If both fail, replace the first test with an equality check against a stored `Color` constant and document the deviation in SUMMARY.md.

    Add the test file + ASSET-PIPELINE.md to pbxproj (test file into the `Travellify` test target's sources-build-phase; the planning doc is not a build artifact but must be committed via the `commit_docs` flow).
  </action>
  <verify>
    `xcodebuild test -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e' -only-testing:TravellifyTests/DesignSystemTokensTests` passes all 5 new tests. Full suite also passes (no regressions).
  </verify>
  <commit>
    feat(07-01): asset-pipeline conventions + design-system smoke tests
  </commit>
</task>

</tasks>

<verification>
**Wave 1 exit gate (all must be TRUE):**

- [ ] `Travellify/DesignSystem/{Tokens,Modifiers,Components}/` directories populated per artifact list
- [ ] `TabBarRoot` is the root view; Trips + Settings tabs visible at launch
- [ ] Tapping Settings shows the "Coming in a future update" placeholder (gear + title + subtitle)
- [ ] Deep-link into a trip (UNNotification response) flips to the Trips tab AND navigates to the trip detail — existing Phase 6 D81 flow unregressed
- [ ] Existing `+` nav-bar toolbar on TripListView is **unchanged** (D7-07 is deferred to 07-02)
- [ ] No v1 requirement regresses — run full `xcodebuild test` suite; expect all Phase 1–6 tests green plus the 5 new DesignSystemTokensTests
- [ ] `ASSET-PIPELINE.md` committed and references the Figma fileKey + expected node-level workflow
- [ ] SUMMARY.md documents any deviations from the exact `@available(iOS 26.0, *)` API signatures (the native Glass API surface is volatile; pick the closest current SDK call and log the choice)
</verification>

<out_of_scope>
- Migrating any feature screen to the new design tokens (that starts in 07-02 Trips).
- Implementing D7-07 (`+` toolbar conditional) — lands with 07-02 since it requires TripListView changes.
- Populating the EmptyStates asset catalog with the actual TripListView illustration — that asset is pulled in 07-02 per the pipeline doc.
- Settings screen content — v1.x.
- Accessibility audit — Phase 8/9.
- Light-mode color variants — deferred (CONTEXT.md §Deferred).
</out_of_scope>
