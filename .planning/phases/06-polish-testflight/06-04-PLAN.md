---
phase: 06-polish-testflight
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json
  - Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png
  - Travellify/PrivacyInfo.xcprivacy
  - Travellify.xcodeproj/project.pbxproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "Assets.xcassets/AppIcon.appiconset/icon-1024.png exists, is exactly 1024×1024, and has NO alpha channel (D85)"
    - "Assets.xcassets/AppIcon.appiconset/Contents.json declares a single-size universal iOS entry with filename='icon-1024.png' (D85)"
    - "Travellify/PrivacyInfo.xcprivacy is valid plist declaring NSPrivacyAccessedAPICategoryUserDefaults/CA92.1 and NSPrivacyAccessedAPICategoryFileTimestamp/C617.1, no tracking, empty collected data (D86)"
    - "PrivacyInfo.xcprivacy has 4 pbxproj entries (PBXBuildFile + PBXFileReference with lastKnownFileType=text.plist.xml + top-level Travellify PBXGroup child + Resources build-phase entry) — NOT Sources (D86)"
    - "MARKETING_VERSION=1.0, CURRENT_PROJECT_VERSION=1, PRODUCT_BUNDLE_IDENTIFIER=com.kespeee.travellify — all verified (D87)"
    - "No Info.plist / INFOPLIST_KEY_* changes (D88 is a no-op; camera + photos keys already present from Phase 2)"
  artifacts:
    - path: "Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png"
      provides: "1024×1024 opaque PNG (placeholder app icon)"
    - path: "Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "iOS 17+ single-size universal icon manifest with filename wired"
      contains: "icon-1024.png"
    - path: "Travellify/PrivacyInfo.xcprivacy"
      provides: "Apple privacy manifest (TN3183 conformant)"
      contains: "CA92.1"
  key_links:
    - from: "Contents.json filename field"
      to: "icon-1024.png"
      via: "asset catalog image reference"
      pattern: "icon-1024\\.png"
    - from: "PBXResourcesBuildPhase (main Travellify target)"
      to: "PrivacyInfo.xcprivacy"
      via: "PBXBuildFile entry"
      pattern: "PrivacyInfo\\.xcprivacy"
---

<objective>
Wave 4 — TestFlight submission minimums. Commit a placeholder 1024×1024 opaque-PNG app icon (D85), create `Travellify/PrivacyInfo.xcprivacy` with Apple TN3183-conformant entries (D86), register the privacy manifest in the Resources build phase (4 pbxproj entries), and verify/adjust version + build + bundle-ID settings if divergent from D87 values. D88 is a no-op — no Info.plist changes required.

Purpose: Closes out all Phase 6 CONTEXT decisions for TestFlight submission (no REQ IDs). No archive or upload — those are manual user-run steps post-phase.
Output: 1 new binary asset (PNG), 1 new plist manifest, 1 modified Contents.json, 4 pbxproj entries for PrivacyInfo.xcprivacy. Two shell smoke checks validate the icon + manifest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-polish-testflight/06-CONTEXT.md
@.planning/phases/06-polish-testflight/06-RESEARCH.md
@.planning/phases/06-polish-testflight/06-PATTERNS.md

<interfaces>
<!-- Existing Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json (current body, per PATTERNS.md + RESEARCH.md §D85 verification):
```json
{
  "images" : [
    {
      "idiom" : "universal",
      "platform" : "ios",
      "size" : "1024x1024"
    }
  ],
  "info" : { "author" : "xcode", "version" : 1 }
}
```
Only change: add `"filename" : "icon-1024.png"` INSIDE the single image object. Do NOT add multiple entries; single-size is the iOS 17+ canonical format. -->

<!-- Apple TN3183 + RESEARCH §D86 (lines 534-565) — verbatim PrivacyInfo.xcprivacy plist content:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>NSPrivacyTracking</key>
  <false/>
  <key>NSPrivacyTrackingDomains</key>
  <array/>
  <key>NSPrivacyCollectedDataTypes</key>
  <array/>
  <key>NSPrivacyAccessedAPITypes</key>
  <array>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>CA92.1</string>
      </array>
    </dict>
    <dict>
      <key>NSPrivacyAccessedAPIType</key>
      <string>NSPrivacyAccessedAPICategoryFileTimestamp</string>
      <key>NSPrivacyAccessedAPITypeReasons</key>
      <array>
        <string>C617.1</string>
      </array>
    </dict>
  </array>
</dict>
</plist>
```
-->

<!-- pbxproj targets (from RESEARCH.md §D87 verification):
- MARKETING_VERSION = 1.0 — confirmed on Debug + Release (pbxproj lines 668, 691, 776, 800)
- CURRENT_PROJECT_VERSION = 1 — confirmed (lines 656, 687, 764, 796)
- PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify — confirmed (lines 669, 692, 777)
If all confirm, Task 3 is verification-only (no pbxproj writes). -->

<!-- PrivacyInfo.xcprivacy pbxproj registration (Resources variant, 4 entries):
1. PBXFileReference — fresh UUID; `lastKnownFileType = text.plist.xml`, `path = PrivacyInfo.xcprivacy`, `sourceTree = "<group>"`.
2. PBXBuildFile — fresh UUID; references the PBXFileReference.
3. Add child entry in the TOP-LEVEL `Travellify/` PBXGroup (alongside ContentView.swift — NOT inside App/ or Shared/; research Pitfall 7).
4. Add BuildFile UUID to the main `Travellify` target's PBXResourcesBuildPhase `files = ( ... )` (at pbxproj line ~470-476, `328E6A41664442069075386D /* Resources */`). NOT the Sources build phase. -->

<!-- Assets.xcassets is ALREADY registered as a Resources input (pbxproj line 13, 474). Adding icon-1024.png INSIDE the existing appiconset does NOT require a new PBXBuildFile/PBXFileReference entry (research landmine #5). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate placeholder 1024×1024 opaque icon-1024.png + wire Contents.json filename</name>
  <read_first>
    - Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json (full file — confirm single-size iOS 17+ schema already present)
    - .planning/phases/06-polish-testflight/06-RESEARCH.md §"D85 — Placeholder app icon" (lines 484-510)
    - .planning/phases/06-polish-testflight/06-PATTERNS.md §"Assets.xcassets/AppIcon.appiconset"
  </read_first>
  <action>
    **Step A — Generate a 1024×1024 opaque PNG placeholder** at `Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png`.

    Use Python + Pillow (if available) OR the macOS `sips` pipeline. Flat yellow background (project's design-system accent `#FFDD2D` is available in MEMORY.md but use any solid opaque color — real branded icon is a v1.1 task) with a bold "T" glyph is suggested but not required; any valid 1024×1024 opaque PNG is acceptable for the TestFlight placeholder.

    One option — create a solid yellow PNG via `sips` + `ImageMagick`/`Pillow`. Lowest-friction approach (Python stdlib + Pillow if available):
    ```bash
    python3 - <<'PY'
    try:
        from PIL import Image, ImageDraw, ImageFont
        img = Image.new('RGB', (1024, 1024), (255, 221, 45))  # opaque — no 'RGBA'
        draw = ImageDraw.Draw(img)
        # Optional: draw 'T' — skip if no font available; solid color is sufficient
        img.save('Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png', 'PNG', optimize=True)
        print("ok")
    except Exception as e:
        print(f"fallback: {e}")
    PY
    ```

    If Pillow is unavailable, fall back to a solid color via `sips` on a generated source (e.g., `printf` a minimal PPM then pipe through `sips -s format png -Z 1024`), or commit a pre-existing solid-color PNG.

    CRITICAL constraints (research Pitfall 6):
    - Exactly 1024×1024 pixels. Verify with `sips -g pixelWidth -g pixelHeight Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png`.
    - NO alpha channel. Verify with `sips -g hasAlpha Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png` — MUST print `hasAlpha: no`. If the generated PNG has alpha (e.g., `Image.new('RGBA', ...)`), flatten via `sips -s format png --setProperty formatOptions normal`.

    **Step B — Update `Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json`** to add the `filename` key inside the existing single image object. Final contents:
    ```json
    {
      "images" : [
        {
          "filename" : "icon-1024.png",
          "idiom" : "universal",
          "platform" : "ios",
          "size" : "1024x1024"
        }
      ],
      "info" : {
        "author" : "xcode",
        "version" : 1
      }
    }
    ```
    Preserve existing key ordering where possible; only insert the `filename` key.

    **NO pbxproj changes** — Assets.xcassets is already registered (research landmine #5).
  </action>
  <verify>
    <automated>test -f Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png && sips -g hasAlpha Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png | grep -q "hasAlpha: no" && sips -g pixelWidth -g pixelHeight Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png | grep -E "pixel(Width|Height): 1024"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png` exit 0
    - `sips -g hasAlpha Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png` stdout contains `hasAlpha: no`
    - `sips -g pixelWidth Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png` stdout contains `pixelWidth: 1024`
    - `sips -g pixelHeight Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png` stdout contains `pixelHeight: 1024`
    - `grep -c "icon-1024.png" Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json` returns 1
    - `grep -c "\"filename\" :" Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json` returns 1
    - `grep -c "1024x1024" Travellify/Assets.xcassets/AppIcon.appiconset/Contents.json` returns 1
    - xcodebuild build succeeds (no "AppIcon is missing a ... variant" warning)
  </acceptance_criteria>
  <done>1024×1024 opaque placeholder PNG committed; Contents.json references it; no pbxproj writes needed; build green without App Store asset warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Create PrivacyInfo.xcprivacy + register 4 pbxproj entries in Resources build phase</name>
  <read_first>
    - .planning/phases/06-polish-testflight/06-RESEARCH.md §"D86 — PrivacyInfo.xcprivacy manifest" (lines 511-574) — verbatim plist contents + pbxproj notes
    - .planning/phases/06-polish-testflight/06-PATTERNS.md §"Travellify/PrivacyInfo.xcprivacy" + §"Pattern: pbxproj 4-entry hand-edit for new Swift file" (adapted to Resources variant)
    - Travellify.xcodeproj/project.pbxproj — locate:
      * `Assets.xcassets` PBXBuildFile + PBXFileReference (as template for resource-type files)
      * Top-level `Travellify/` PBXGroup children list (where ContentView.swift is a sibling — PrivacyInfo.xcprivacy goes HERE, not inside App/ or Shared/)
      * Main target PBXResourcesBuildPhase (search for `328E6A41664442069075386D /* Resources */` around line 470-476)
  </read_first>
  <action>
    **Step A — Create `Travellify/PrivacyInfo.xcprivacy`** with the VERBATIM plist from RESEARCH §D86 lines 534-565:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
      <key>NSPrivacyTracking</key>
      <false/>
      <key>NSPrivacyTrackingDomains</key>
      <array/>
      <key>NSPrivacyCollectedDataTypes</key>
      <array/>
      <key>NSPrivacyAccessedAPITypes</key>
      <array>
        <dict>
          <key>NSPrivacyAccessedAPIType</key>
          <string>NSPrivacyAccessedAPICategoryUserDefaults</string>
          <key>NSPrivacyAccessedAPITypeReasons</key>
          <array>
            <string>CA92.1</string>
          </array>
        </dict>
        <dict>
          <key>NSPrivacyAccessedAPIType</key>
          <string>NSPrivacyAccessedAPICategoryFileTimestamp</string>
          <key>NSPrivacyAccessedAPITypeReasons</key>
          <array>
            <string>C617.1</string>
          </array>
        </dict>
      </array>
    </dict>
    </plist>
    ```

    **Step B — Register 4 pbxproj entries (Resources variant).**

    Generate two fresh 24-hex-char UUIDs in the AD-prefixed family already used (search `grep "AD.......*/ \* .* \*/" Travellify.xcodeproj/project.pbxproj` for examples). Call them UUID_A (PBXBuildFile) and UUID_B (PBXFileReference).

    1. **PBXFileReference** — add to the `/* Begin PBXFileReference section */ ... /* End PBXFileReference section */` block:
       ```
       UUID_B /* PrivacyInfo.xcprivacy */ = {isa = PBXFileReference; lastKnownFileType = text.plist.xml; path = PrivacyInfo.xcprivacy; sourceTree = "<group>"; };
       ```

    2. **PBXBuildFile** — add to the `/* Begin PBXBuildFile section */ ... /* End PBXBuildFile section */` block:
       ```
       UUID_A /* PrivacyInfo.xcprivacy in Resources */ = {isa = PBXBuildFile; fileRef = UUID_B /* PrivacyInfo.xcprivacy */; };
       ```

    3. **PBXGroup child** — add UUID_B to the TOP-LEVEL `Travellify/` PBXGroup's `children = ( ... )` list. This is the group whose children include `ContentView.swift`, `App/`, `Features/`, `Models/`, `Services/`, `Shared/`, `Assets.xcassets`. PrivacyInfo.xcprivacy is a SIBLING of ContentView.swift — NOT inside any subgroup (research landmine #5 / Pitfall 7 — placing it in a subgroup causes it to be copied to a non-bundle-root location and fail App Store validation).
       Add line:
       ```
       UUID_B /* PrivacyInfo.xcprivacy */,
       ```
       alongside the `ContentView.swift` child entry.

    4. **PBXResourcesBuildPhase** — locate the main `Travellify` target's Resources build phase (search `/* Resources */ = { isa = PBXResourcesBuildPhase;`; there are usually two — the main target's and the test target's; choose the main `Travellify` app target around pbxproj line 470-476). Add UUID_A to its `files = ( ... )` list alongside the existing `Assets.xcassets in Resources` entry:
       ```
       UUID_A /* PrivacyInfo.xcprivacy in Resources */,
       ```

    CRITICAL — add to Resources phase, NOT Sources phase (PrivacyInfo.xcprivacy is a bundle resource, not a source file).

    Validate the pbxproj still parses by attempting an xcodebuild build. If the project does not open cleanly, revert and retry.
  </action>
  <verify>
    <automated>plutil -lint Travellify/PrivacyInfo.xcprivacy && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -project Travellify.xcodeproj -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Travellify/PrivacyInfo.xcprivacy` exit 0
    - `plutil -lint Travellify/PrivacyInfo.xcprivacy` prints `OK`
    - `grep -c "CA92.1" Travellify/PrivacyInfo.xcprivacy` returns 1
    - `grep -c "C617.1" Travellify/PrivacyInfo.xcprivacy` returns 1
    - `grep -c "NSPrivacyAccessedAPICategoryUserDefaults" Travellify/PrivacyInfo.xcprivacy` returns 1
    - `grep -c "NSPrivacyAccessedAPICategoryFileTimestamp" Travellify/PrivacyInfo.xcprivacy` returns 1
    - `grep -c "<key>NSPrivacyTracking</key>" Travellify/PrivacyInfo.xcprivacy` returns 1
    - `grep -c "PrivacyInfo.xcprivacy" Travellify.xcodeproj/project.pbxproj` returns 4
    - `grep -c "text.plist.xml.*PrivacyInfo.xcprivacy" Travellify.xcodeproj/project.pbxproj` returns 1 (PBXFileReference line)
    - `grep -c "PrivacyInfo.xcprivacy in Resources" Travellify.xcodeproj/project.pbxproj` returns 2 (PBXBuildFile declaration + Resources phase usage)
    - xcodebuild build exits 0
  </acceptance_criteria>
  <done>PrivacyInfo.xcprivacy committed at Travellify/ root; plutil validates; 4 pbxproj entries wire it into the Resources build phase (NOT Sources); build green.</done>
</task>

<task type="auto">
  <name>Task 3: Verify MARKETING_VERSION / CURRENT_PROJECT_VERSION / PRODUCT_BUNDLE_IDENTIFIER; confirm D88 no-op</name>
  <read_first>
    - Travellify.xcodeproj/project.pbxproj (search for `MARKETING_VERSION`, `CURRENT_PROJECT_VERSION`, `PRODUCT_BUNDLE_IDENTIFIER` occurrences — expect 2 configs each on the main target: Debug + Release)
    - .planning/phases/06-polish-testflight/06-RESEARCH.md §"D87 — Version / build / bundle ID" (lines 576-585) — confirms these values already match
    - .planning/phases/06-polish-testflight/06-CONTEXT.md D87, D88
  </read_first>
  <action>
    **Step A — Verification grep** (no-op if all three values match the target).

    Run three greps:
    ```bash
    grep -c "MARKETING_VERSION = 1.0" Travellify.xcodeproj/project.pbxproj
    grep -c "CURRENT_PROJECT_VERSION = 1;" Travellify.xcodeproj/project.pbxproj
    grep -c "PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify" Travellify.xcodeproj/project.pbxproj
    ```

    Expected values (per RESEARCH.md):
    - `MARKETING_VERSION = 1.0` — appears on main target Debug + Release AND test target Debug + Release → expected count ≥ 2. The main target's 2 occurrences are the ones that matter for submission.
    - `CURRENT_PROJECT_VERSION = 1;` — same pattern → expected count ≥ 2.
    - `PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify` (main target, 2 configs) AND `com.kespeee.travellify.TravellifyTests` (test target, 2 configs). Main identifier must appear at least 2 times with EXACTLY `com.kespeee.travellify` (not the test variant). Use an exact-match grep:
    ```bash
    grep -c "PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify;" Travellify.xcodeproj/project.pbxproj
    ```
    (Trailing semicolon isolates main-app lines from test-target lines that have `.TravellifyTests;`.)

    **Step B — If any of the three grep counts diverges from expected (per RESEARCH)**:
    - For `MARKETING_VERSION`: edit any non-1.0 line on the main app target configs to `MARKETING_VERSION = 1.0;`.
    - For `CURRENT_PROJECT_VERSION`: edit to `CURRENT_PROJECT_VERSION = 1;`.
    - For `PRODUCT_BUNDLE_IDENTIFIER`: edit to `PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify;` on the main app target only (leave `com.kespeee.travellify.TravellifyTests;` on test-target configs untouched).

    Do NOT change test-target bundle IDs. Do NOT change code-signing style (leave Automatic). Do NOT trigger archive or upload.

    **Step C — D88 no-op verification.**
    Confirm NO new INFOPLIST_KEY_* additions are needed:
    - `NSUserNotificationsUsageDescription` must NOT be present (macOS-only, research D88). Grep must return 0.
    - `NSCameraUsageDescription` (via `INFOPLIST_KEY_NSCameraUsageDescription`) should already be present from Phase 2.
    - `NSPhotoLibraryUsageDescription` is NOT required (PhotosPicker runs out-of-process).

    If any violation surfaces (e.g., someone added `NSUserNotificationsUsageDescription` by mistake), delete it.
  </action>
  <verify>
    <automated>grep -c "MARKETING_VERSION = 1.0" Travellify.xcodeproj/project.pbxproj && grep -c "CURRENT_PROJECT_VERSION = 1;" Travellify.xcodeproj/project.pbxproj && grep -c "PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify;" Travellify.xcodeproj/project.pbxproj && ! grep -q "NSUserNotificationsUsageDescription" Travellify.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MARKETING_VERSION = 1.0" Travellify.xcodeproj/project.pbxproj` returns ≥ 2
    - `grep -c "CURRENT_PROJECT_VERSION = 1;" Travellify.xcodeproj/project.pbxproj` returns ≥ 2
    - `grep -c "PRODUCT_BUNDLE_IDENTIFIER = com.kespeee.travellify;" Travellify.xcodeproj/project.pbxproj` returns ≥ 2 (main-app configs)
    - `grep -c "NSUserNotificationsUsageDescription" Travellify.xcodeproj/project.pbxproj` returns 0
    - `grep -c "INFOPLIST_KEY_NSCameraUsageDescription" Travellify.xcodeproj/project.pbxproj` returns ≥ 2 (preserved from Phase 2)
    - If no divergence from RESEARCH.md values found, NO pbxproj writes occurred in Task 3
    - xcodebuild build still exits 0 after any corrective edits
  </acceptance_criteria>
  <done>Version + build + bundle-ID values verified at D87 targets (1.0 / 1 / com.kespeee.travellify); no Info.plist / INFOPLIST_KEY additions (D88 no-op); test-target bundle IDs untouched.</done>
</task>

</tasks>

<verification>
Shell smoke checks (Wave 4 has no Swift tests — per 06-VALIDATION.md):
```
plutil -lint Travellify/PrivacyInfo.xcprivacy
sips -g hasAlpha Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png   # expect: hasAlpha: no
sips -g pixelWidth -g pixelHeight Travellify/Assets.xcassets/AppIcon.appiconset/icon-1024.png   # expect: 1024 / 1024
```

Full build + test (regression gate — Wave 1-3 suites must all still pass):
```
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -project Travellify.xcodeproj -scheme Travellify -destination 'platform=iOS Simulator,name=iPhone 16e'
```

Manual iPhone 16e smoke (pre `/gsd-verify-work`):
1. Launch app — app icon on home screen is the flat placeholder (not the blank template).
2. Archive-preflight in Xcode (Product → Archive) — no "missing privacy manifest" or "AppIcon missing variant" warnings. (Do NOT actually run Archive upload — user-run step.)

Full Wave 0 validation gap closure (from 06-VALIDATION.md):
- [x] TripReminderFireDateTests (Wave 2)
- [x] ReminderSchemaTests.tripReminderDefaults (Wave 2)
- [x] ReminderLifecycleTests trip tests (Wave 3)
- [x] NotificationSchedulerTests union + prefix tests (Wave 3)
- [x] ImportTests.defaultName* (Wave 1)
- [x] ActivityTests.outsideRangeDayLevel (Wave 1)
- [x] Shell checks for D85/D86 (this wave)
</verification>

<success_criteria>
- D85: `icon-1024.png` exists at exactly 1024×1024 with NO alpha channel; Contents.json references it via `filename` key
- D86: `Travellify/PrivacyInfo.xcprivacy` valid per `plutil -lint`; declares CA92.1 (UserDefaults) + C617.1 (FileTimestamp); tracking false; collected data empty
- D86 pbxproj: 4 entries for PrivacyInfo.xcprivacy in Resources phase (NOT Sources); file referenced as `text.plist.xml` under top-level `Travellify/` PBXGroup
- D87: MARKETING_VERSION=1.0 + CURRENT_PROJECT_VERSION=1 + PRODUCT_BUNDLE_IDENTIFIER=com.kespeee.travellify verified on main target
- D88: no-op — no new INFOPLIST_KEY_* keys; no NSUserNotificationsUsageDescription
- Full test suite still green; build green; archive-preflight clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-testflight/06-04-SUMMARY.md` documenting:
- The exact PNG generation tool used (Pillow / sips / other) and the resulting file size
- Confirmation of `hasAlpha: no` from sips
- pbxproj UUIDs generated for PrivacyInfo.xcprivacy (both PBXBuildFile + PBXFileReference)
- Confirmation that PrivacyInfo.xcprivacy landed in the TOP-LEVEL Travellify PBXGroup (sibling of ContentView.swift) — NOT inside App/ or Shared/
- D87 pre-edit grep counts for MARKETING_VERSION / CURRENT_PROJECT_VERSION / PRODUCT_BUNDLE_IDENTIFIER; document whether any corrective writes were needed
- Any archive-preflight warnings observed in Xcode (for follow-up in a v1.1 plan)
- Reminder: user-run manual steps post-phase — select Apple Developer team in Xcode signing, Product → Archive, upload to App Store Connect, add TestFlight internal testers
</output>
